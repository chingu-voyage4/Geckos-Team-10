import React from "react";

const styles = {
  display: "flex",
  justifyContent: "center",
  marginTop: "25vh"
};

const Pricing = () => (
  <main style={styles}>
    <h1>This is the Pricing page</h1>
  </main>
);

export default Pricing;
